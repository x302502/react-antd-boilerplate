// Define the field name used in HTTP request headers for transmitting authorization information
export const AUTH_HEADER = "Authorization";

// Define the field name used in HTTP request headers for specifying language preferences
export const LANG_HEADER = "X-Lang";

// Define the field name used in HTTP request headers for specifying the application name
export const APP_NAME_HEADER = "X-App-Name";
